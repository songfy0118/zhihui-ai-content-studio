# 知绘工厂 · 抖音草稿连接器

这是一个本机 Chrome/Edge 扩展，只做一件事：把知绘工厂当前生成的标题、摘要和正文填进当前已登录的抖音文章编辑器。

它不会读取密码，不会登录，不会保存草稿，也不会点击发布。

## 安装

1. 打开 `chrome://extensions` 或 `edge://extensions`。
2. 开启“开发者模式”。
3. 点击“加载已解压的扩展程序”，选择本目录。
4. 回到知绘工厂，生成草稿后点击“自动填入抖音（不发布）”。

抖音创作页会在新标签页打开并自动填入。检查内容、头图和封面后，仍由账号本人决定是否保存或发布。
